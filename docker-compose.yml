version: "3"
services:
  jupyter:
    build: .
    container_name: kernel
    volumes:
      - $PWD:/tmp/working
    working_dir: /tmp/working
    ports:
      - 8888:8888
    restart: always

    command: jupyter lab --ip=0.0.0.0 --allow-root --no-browser